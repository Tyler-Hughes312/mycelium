"""Mycelium Core — local-first Context Layer HTTP API + MCP.

Install (editable)::

    pip install -e "services/core[dev]"

Run::

    mycelium serve          # production (no reload)
    mycelium-core           # alias
    mycelium-mcp            # MCP stdio bridge (requires Core running)

Docs: see repository README and docs/DEPLOY.md.
"""
