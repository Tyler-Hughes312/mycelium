# Story 8.1: One-command local install + README

Status: done

## Delivered

- `scripts/install.sh` — venv, Core `[dev]`, Desktop + VS Code npm, dogfood fixture
- Root `README.md` — privacy-first, `<15 min` path via install → `./scripts/dev.sh`
- Points to demo, dogfood checklist, MCP, embedding eval
