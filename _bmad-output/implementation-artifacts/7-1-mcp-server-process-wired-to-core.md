# Story 7.1: MCP server process wired to Core

Status: done

## Delivered

- `python -m mycelium.bridges.mcp` / `mycelium-mcp` (stdio FastMCP)
- Calls same Core HTTP API (AD-3)
- README + `.cursor/mcp.json.example` for Cursor / Claude Code
