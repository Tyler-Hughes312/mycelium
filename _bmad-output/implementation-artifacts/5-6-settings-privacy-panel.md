# Story 5.6: Settings & privacy panel

Status: done

## Delivered

- `GET/PATCH /settings` persists `config.toml`
- Settings UI: vault path, history depth, embedding model, privacy summary
- Hot-swap vault path + history depth; embedding model change hints Core restart
